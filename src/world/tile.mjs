export const FLOOR = Object.freeze({
  glyph: ".",
  walkable: true,
  transparent: true,
  color: "white",
});

export const WALL = Object.freeze({
  glyph: "#",
  walkable: false,
  transparent: false,
  color: "white",
});

export const DOOR = Object.freeze({
  glyph: "+",
  walkable: true,
  transparent: false,
  color: "brown",
});
